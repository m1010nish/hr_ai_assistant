# Base URL of the Express API, including the /api prefix.
# The browser talks only to this service — MongoDB, Cloudinary and Gemini are
# reached by the backend, so no provider credentials belong in this file.
NEXT_PUBLIC_API_URL=http://localhost:8080/api
